# КАСБОТ — зависимости
# Установка: pip install -r requirements.txt

ultralytics>=8.2.0        # YOLOv8n + автозагрузка весов
opencv-python>=4.8.0      # захват видео + рендеринг UI
numpy>=1.24.0             # матричные операции (HSV, маски)
mediapipe>=0.10.0         # трекинг рук (опционально, но рекомендуется)
